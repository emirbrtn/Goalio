# Deploy

1. Backend:
   - `cd backend`
   - `npm install`
   - `npm run start`

2. Frontend:
   - `cd frontend`
   - `npm install`
   - `npm run dev`

3. Ortam değişkenleri:
   - Backend: `.env` içinde `PORT`, `JWT_SECRET`, `DB_FILE`
   - Frontend: `.env.local` içinde `NEXT_PUBLIC_API_URL`
